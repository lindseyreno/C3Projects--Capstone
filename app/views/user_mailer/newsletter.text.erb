My Seattle Events - <%= @schedule %>

Hi <%= @user.username %>

<% @categories.each do |category| %>
  <%= category.name %>
  <% category.events.send("#{@plan}").each do |event| %>
    <%= event.title %> - <%= pretty_date(event.date) %>
  <% end %>
<% end %>
